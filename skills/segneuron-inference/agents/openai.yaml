interface:
  display_name: "SegNeuron Inference"
  short_description: "Inspect and verify SegNeuron neuron instances"
  default_prompt: "Use $segneuron-inference for the requested SegNeuron task, reusing existing artifacts and running only the necessary audit, inference, reconstruction, visualization, or verification stages."
