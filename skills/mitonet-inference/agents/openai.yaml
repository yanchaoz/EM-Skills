interface:
  display_name: "MitoNet Inference"
  short_description: "Audit, run, and verify mitochondrial instance segmentation"
  default_prompt: "Use $mitonet-inference to audit this volume EM dataset, run a MitoNet pilot, and verify mitochondrial instances."
