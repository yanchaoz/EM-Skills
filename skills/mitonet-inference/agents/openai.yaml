interface:
  display_name: "MitoNet Inference"
  short_description: "Run, inspect, and verify MitoNet segmentation"
  default_prompt: "Use $mitonet-inference for the requested mitochondrial segmentation task, reusing existing artifacts and running only the necessary preparation, inference, visualization, or verification stages."
